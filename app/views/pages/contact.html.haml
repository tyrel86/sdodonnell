#content
	- if notice
		%p.notice= notice
	- if alert
		%p.error= alert
	%h1 Contact
	= simple_form_for @contact do |f|
		= f.input :name
		= f.input :email
		= f.input :message, as: :text
		= f.submit "Send Message"

= render "partials/dbside_add"
