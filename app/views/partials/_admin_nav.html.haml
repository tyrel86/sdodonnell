#nav
	%ul
		%li{class: ("active" if active?('home'))}= link_to "Social/Blog", new_blog_path
		%li{class: ("active" if active?('home'))}= link_to "Events", new_event_path
		%li{class: ("active" if active?('home'))}= link_to "Mailer", "#"
